name: Publish to Itch.io

on:
 push:
  branches:
  - main
 pull_request:
  branches:
  - main

permissions:
  contents: write
  actions: write

env:
 BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }} # Needs to be added as a repository secret
 GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
 IS_MAIN: ${{ contains(github.ref, 'main') }}
 ITCH_USER: ${{ vars.ITCH_USER }} # Your itch.io username - needs to be added as a repository variable
 ITCH_GAME: ${{ vars.ITCH_GAME }} # Your game name on itch.io - needs to be added as a repository variable

jobs:
 Detect-Platforms:
  runs-on: ubuntu-latest
  outputs:
    matrix: ${{ steps.set-matrix.outputs.matrix }}
    has-platforms: ${{ steps.set-matrix.outputs.has-platforms }}
    godot-version: ${{ steps.detect-config.outputs.godot-version }}
    game-name: ${{ steps.detect-config.outputs.game-name }}
  steps:
  - uses: actions/checkout@v4
  
  - name: Detect Project Configuration
    id: detect-config
    shell: bash
    run: |
      echo "Detecting project configuration from project.godot..."
      
      # Extract Godot version from project.godot (just the version number like "4.3" or "4.5")
      GODOT_VERSION=$(grep 'config/features' project.godot | grep -o '[0-9]\+\.[0-9]\+' | head -n 1)
      
      # Fail if version not found
      if [ -z "$GODOT_VERSION" ]; then
        echo "❌ ERROR: Could not detect Godot version from project.godot"
        exit 1
      fi
      
      # Extract game name from project.godot (fallback to repo name)
      GAME_NAME=$(grep 'config/name=' project.godot | sed 's/config\/name="\(.*\)"/\1/' | tr ' ' '-' | tr '[:upper:]' '[:lower:]' || echo "godot-game")
      
      echo "godot-version=$GODOT_VERSION" >> $GITHUB_OUTPUT
      echo "game-name=$GAME_NAME" >> $GITHUB_OUTPUT
      
      echo "Detected configuration:"
      echo "  Godot Version: $GODOT_VERSION"
      echo "  Game Name: $GAME_NAME"
  
  - name: Detect Export Presets & Set Matrix
    id: set-matrix
    shell: bash
    run: |
      echo "Detecting configured export presets..."
      
      # Get game name from previous step
      GAME_NAME="${{ steps.detect-config.outputs.game-name }}"
      
      # Define platform configurations
      declare -A platforms=(
        ["windows"]="Windows Desktop|windows|${GAME_NAME}.exe"
        ["linux"]="Linux|linux|${GAME_NAME}.x86_64"
        ["web"]="Web|web|index.html"
        ["macos"]="macOS|macos|${GAME_NAME}.app"
      )
      
      # Build matrix array
      matrix_include="["
      platform_count=0
      
      for platform in "${!platforms[@]}"; do
        IFS='|' read -ra PLATFORM_INFO <<< "${platforms[$platform]}"
        preset_name="${PLATFORM_INFO[0]}"
        folder_name="${PLATFORM_INFO[1]}"
        file_name="${PLATFORM_INFO[2]}"
        
        # Check if preset exists
        if grep -q "platform=\"$preset_name\"" export_presets.cfg; then
          echo "✅ Found preset for $platform ($preset_name)"
          
          if [ $platform_count -gt 0 ]; then
            matrix_include+=","
          fi
          
          matrix_include+="{\"platform\":\"$platform\",\"preset\":\"$preset_name\",\"folder\":\"$folder_name\",\"file\":\"$file_name\"}"
          platform_count=$((platform_count + 1))
        else
          echo "❌ No preset found for $platform ($preset_name)"
        fi
      done
      
      matrix_include+="]"
      
      if [ $platform_count -eq 0 ]; then
        echo "No export presets found!"
        echo "has-platforms=false" >> $GITHUB_OUTPUT
        echo "matrix={\"include\":[]}" >> $GITHUB_OUTPUT
      else
        echo "Found $platform_count platform(s) to export"
        echo "has-platforms=true" >> $GITHUB_OUTPUT
        echo "matrix={\"include\":$matrix_include}" >> $GITHUB_OUTPUT
      fi
      
      echo "Matrix: {\"include\":$matrix_include}"

 Export:
  needs: Detect-Platforms
  if: needs.Detect-Platforms.outputs.has-platforms == 'true'
  runs-on: ubuntu-latest
  strategy:
    matrix: ${{ fromJson(needs.Detect-Platforms.outputs.matrix) }}
    fail-fast: false  # Continue other exports even if one fails
  steps:
  - uses: actions/checkout@v4
  
  # Cache Godot engine and templates
  - name: Cache Godot
    uses: actions/cache@v4
    id: cache-godot
    with:
      path: |
        godot
        ~/.local/share/godot/export_templates
      key: godot-${{ needs.Detect-Platforms.outputs.godot-version }}-stable
  
  - name: Setup Godot
    if: steps.cache-godot.outputs.cache-hit != 'true'
    shell: bash
    run: |
       GODOT_VERSION="${{ needs.Detect-Platforms.outputs.godot-version }}"
       echo "Setting up Godot $GODOT_VERSION..."
       
       # Download Godot from GitHub releases
       echo "Downloading Godot executable from GitHub..."
       GODOT_URL="https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
       echo "URL: $GODOT_URL"
       
       if ! curl -f -L -o godot.zip "$GODOT_URL"; then
         echo "❌ ERROR: Failed to download Godot $GODOT_VERSION from GitHub"
         echo "Please check if version $GODOT_VERSION-stable exists at:"
         echo "https://github.com/godotengine/godot/releases"
         exit 1
       fi
       
       unzip -q godot.zip
       mv "Godot_v${GODOT_VERSION}-stable_linux.x86_64" godot
       chmod +x godot
       ./godot --version
       
       # Download export templates from GitHub releases
       echo "Downloading export templates from GitHub..."
       TEMPLATES_URL="https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_export_templates.tpz"
       echo "URL: $TEMPLATES_URL"
       
       if ! curl -f -L -o export_templates.tpz "$TEMPLATES_URL"; then
         echo "❌ ERROR: Failed to download export templates for Godot $GODOT_VERSION"
         echo "Please check if export templates exist for version $GODOT_VERSION-stable"
         exit 1
       fi
       
       unzip -q export_templates.tpz
       
       # Install templates with the correct version format (e.g., 4.5.stable)
       mkdir -p ~/.local/share/godot/export_templates/$GODOT_VERSION.stable
       mv ./templates/* ~/.local/share/godot/export_templates/$GODOT_VERSION.stable/
       
       # Verify templates installed
       echo "✅ Successfully installed Godot $GODOT_VERSION and export templates"
       echo "Templates installed at: ~/.local/share/godot/export_templates/$GODOT_VERSION.stable/"
       ls -la ~/.local/share/godot/export_templates/$GODOT_VERSION.stable/ | head -n 10

  - name: Export ${{ matrix.platform }}
    shell: bash
    run: |
      mkdir -p ./exports/${{ matrix.folder }}
      echo "Exporting ${{ matrix.platform }} build..."
      
      ./godot --headless --path ./ --export-release "${{ matrix.preset }}" "./exports/${{ matrix.folder }}/${{ matrix.file }}"
      
      # Verify export
      if [ "${{ matrix.platform }}" == "macos" ]; then
        # macOS exports as .app directory
        if [ ! -d "./exports/${{ matrix.folder }}/${{ matrix.file }}" ]; then
          echo "❌ ${{ matrix.platform }} export failed"
          exit 1
        fi
      else
        # Other platforms export as files
        if [ ! -f "./exports/${{ matrix.folder }}/${{ matrix.file }}" ]; then
          echo "❌ ${{ matrix.platform }} export failed"
          exit 1
        fi
      fi
      
      echo "✅ ${{ matrix.platform }} export completed successfully"
      ls -la "./exports/${{ matrix.folder }}/"
  
  - name: Upload ${{ matrix.platform }} Export
    uses: actions/upload-artifact@v4
    with:
     name: export-${{ matrix.platform }}
     path: exports/${{ matrix.folder }}
  
 Release:
  needs: Export
  if: ${{ contains(github.ref, 'main') }}
  runs-on: ubuntu-latest
  outputs:
    release-tag: ${{ steps.tag_version.outputs.new_tag }}
  steps:
    - uses: actions/checkout@v4
   
    - name: Version
      id: tag_version
      uses: mathieudutour/github-tag-action@v6.2
      with:
       github_token: ${{ secrets.GITHUB_TOKEN }}
       default_bump: false
       release_branches: main
    
    - name: Verify Tag
      if: ${{ !steps.tag_version.outputs.new_tag }}
      run: exit 1
    
    - name: Download Exports
      uses: actions/download-artifact@v4
      with:
       pattern: export-*
       path: exports
       merge-multiple: true
    
    - name: Create Platform Zips
      run: |
        # Create ZIPs for downloadable platforms (not web)
        [ -d exports/windows ] && zip -r godot-brackeys-tutorial-windows-${{ steps.tag_version.outputs.new_tag }}.zip exports/windows
        [ -d exports/linux ] && zip -r godot-brackeys-tutorial-linux-${{ steps.tag_version.outputs.new_tag }}.zip exports/linux
        [ -d exports/macos ] && zip -r godot-brackeys-tutorial-macos-${{ steps.tag_version.outputs.new_tag }}.zip exports/macos
        
        # List created files
        ls -lh *.zip 2>/dev/null || echo "No ZIP files created"
    
    - name: Create Github Release
      id: create_release
      uses: softprops/action-gh-release@v1
      env:
       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
       tag_name: ${{ steps.tag_version.outputs.new_tag }}
       name: Release ${{ steps.tag_version.outputs.new_tag }}
       body: ${{ steps.tag_version.outputs.changelog }}
       files: |
         godot-brackeys-tutorial-*-${{ steps.tag_version.outputs.new_tag }}.zip
 
 Publish:
  needs: Release
  if: ${{ contains(github.ref, 'main') && needs.Release.outputs.release-tag }}
  runs-on: ubuntu-latest
  strategy:
    matrix:
      include:
        - platform: windows
          folder: windows
        - platform: linux
          folder: linux
        - platform: web
          folder: web
    fail-fast: false
  steps:
    - name: Download ${{ matrix.platform }} Export
      uses: actions/download-artifact@v4
      with:
        name: export-${{ matrix.platform }}
        path: exports/${{ matrix.folder }}
    
    - name: Cache Butler
      uses: actions/cache@v4
      id: cache-butler
      with:
        path: butler
        key: butler-latest
    
    - name: Download + Setup Butler
      if: steps.cache-butler.outputs.cache-hit != 'true'
      shell: bash
      run: |
       echo "Downloading Butler..."
       curl -L -o butler.zip https://broth.itch.ovh/butler/linux-amd64/LATEST/archive/default
       unzip butler.zip
       chmod +x butler
    
    - name: Verify Butler
      shell: bash
      run: |
        ./butler -V
        echo "Butler version verified"
    
    - name: Login To Butler
      run: ./butler login
      env:
        BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
    
    - name: Push ${{ matrix.platform }} Build to Itch.io
      shell: bash
      run: |
        echo "Pushing ${{ matrix.platform }} build..."
        ./butler push exports/${{ matrix.folder }} ${{ env.ITCH_USER }}/${{ env.ITCH_GAME }}:${{ matrix.platform }} --userversion ${{ needs.Release.outputs.release-tag }}
        echo "${{ matrix.platform }} build pushed successfully"
    

 Notify:
  needs: [Export, Release, Publish]
  if: always() && contains(github.ref, 'main')
  runs-on: ubuntu-latest
  steps:
    - name: Notify Success
      if: ${{ needs.Export.result == 'success' && needs.Release.result == 'success' && needs.Publish.result == 'success' }}
      shell: bash
      run: |
        echo "🎉 Deployment successful!"
        echo "Release: ${{ needs.Release.outputs.release-tag }}"
        echo "All platforms have been published to itch.io!"
    
    - name: Notify Failure
      if: ${{ needs.Export.result == 'failure' || needs.Release.result == 'failure' || needs.Publish.result == 'failure' }}
      shell: bash
      run: |
        echo "❌ Deployment failed!"
        echo "Export: ${{ needs.Export.result }}"
        echo "Release: ${{ needs.Release.result }}"
        echo "Publish: ${{ needs.Publish.result }}"

 Cleanup:
  needs: [Export, Release, Publish, Notify]
  if: always()
  runs-on: ubuntu-latest
  steps:
    - uses: geekyeggo/delete-artifact@v5
      with:
       name: |
         export-*